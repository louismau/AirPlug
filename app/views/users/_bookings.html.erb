<h1>My bookings</h1>
<ul>
  <% @user.bookings.each do |booking| %>
    <li><%= link_to booking.plug, plug_path(booking.plug) %></li>
    <li><%= "From #{booking.start_time} to #{booking.end_time}" %></li>
    <li><%= booking.transaction_price %></li>
  <% end %>
</ul>
